@model LearnEXMProject.Models.SitecoreCinema.RegisterViewModel

This is going to be the register page.
<br />

@if (Model != null)
{
  <h3>Thank you for registering<strong>  @Model.CandidateContactInfo.FirstName @Model.CandidateContactInfo.LastName </strong>.</h3>
  <br/>
  <p>We'll be sending you a confirmation email as soon as Gregory codes that in.</p>

  <a href="@CinemaConst.Links.SitecoreCinema.SelfServiceMachine" target="_self"> Self Service Machine</a>
  <br />
}else
{ 

<h3>Null model</h3>

}